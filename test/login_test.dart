/*import 'package:myuseum/login.dart';
import 'package:test/test.dart';

void main() {
  group('fromJson split tests', () {
    test('accessToken json splitting works', () {
      Map<String, dynamic> json = {'accessToken': '200', 'id': '3', 'email': 'test@email.com'};
      final Login login = new Login.fromJson(json);
      expect(login.accessToken, '200');
    });

    test('id json splitting works', () {
      Map<String, dynamic> json = {'accessToken': '200', 'id': '3', 'email': 'test@email.com'};
      final Login login = new Login.fromJson(json);
      expect(login.id, '3');
    });

    test('email json splitting works', () {
      Map<String, dynamic> json = {'accessToken': '200', 'id': '3', 'email': 'test@email.com'};
      final Login login = new Login.fromJson(json);
      expect(login.email, 'test@email.com');
    });
  });
}*/